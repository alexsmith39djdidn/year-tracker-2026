/* ═══════════════════════════════════════════════════════════
   ДАННЫЕ КАРТЫ ПРОКАЧКИ САШИ — единственное место для правок.
   Все три виджета (общий рейтинг, радар, области) читают отсюда.

   Как править:
   - score: балл 0–100, или null если не измеряли (покажется «—»)
   - value: что показать текстом (факт). Для сырых показателей — само число
   - raw:   true → это не балл, а просто величина (вес, обхваты)
   - stale: true → замер устарел, в балл группы не идёт (правило свежести
            из skill rating-calc: поведение 14 дней, физика 45 дней)
   - est:   true → это оценка Auri, а не замер (энергия, резерв). Считается
            наравне, но помечается — чтобы впечатление не путать с данными

   Индекс и покрытие считаются автоматически — руками не проставлять.
   ═══════════════════════════════════════════════════════════ */

var RATING = {
  version: 'v0.4',
  date: '16.08.2026',
  window: 'неделя 10–16.08.2026 (⚠️ откат 10–13.08 · 16.08 не дожит, вычтен из знаменателя)',
  // Индекс карты v0.3 (09.08.2026) = 59. Сравниваем с ним.
  // ⚠️ ОГОВОРКА v0.4: 10–12.08 восстановлены задним числом расширенным опросом,
  // часть блока сознательно не спрашивалась — это дыра сбора, не нули.
  // Спорт — объективный Notion Workout Log, там нули настоящие (0 из 12 за 10–13.08).
  // 🆕 Правило от 16.08: шкала со смещённой выборкой (данные только за хорошие дни)
  // помечается stale и в групповой балл не идёт — как просроченный замер.
  prev: 59,

  groups: [
    {
      key: 'core', name: 'Интегральные', icon: '◆',
      scales: [
        { name: 'Резерв',            value: '≈58 · откат съел, вернулось не всё', score: 58, est: true },
        { name: 'Энергия Current',   value: '≈46 · по дням 25·20·45·35·72·80', score: 46, est: true },
        // Дневные баллы продуктивности не собирались — считать не из чего. Прочерк, не ноль.
        { name: 'Продуктивность дня',value: 'не собиралась',  score: null },
        // ✅ Февральский откис (16.02) перешёл 180 дней — штраф −10 снят автоматически.
        // Новый откис 10–13.08: 4 дня подряд = глубокий, −25.
        // 100 − 25 (июнь) − 0 (февраль) − 8 (27.07) − 8 (01.08) − 25 (10–13.08) = 34.
        { name: 'Дни откиса',        value: '5 за полгода · новый 10–13.08 (4 дня)', score: 34 }
      ]
    },
    {
      key: 'stability', name: 'Стабильность', icon: '⏱',
      scales: [
        // отбой в окне 1 из 3 измеримых · подъём 3 из 6 · длина сна 72 (ночь 12 ч дала 15)
        // 33×0.4 + 50×0.4 + 72×0.2 = 47.6
        { name: 'Стабильность сна',  value: 'подъём 6:15–10:00 · ночи 7:00–12:00', score: 48 },
        { name: 'Ровность дня',      value: 'точек дня недостаточно',  score: null },
        // Впервые вошли часы работы (43% от плана) — рост 10 → 34 это честность цифры,
        // а не улучшение поведения. 10 линий: 33·0·33·33·0·67·17·60·50·43 → 33.6
        { name: 'Стабильность работы',value: '34% по 10 линиям · работа 20.75 из 55 ч', score: 34 },
        { name: 'Настроение',        value: '3.7 из 5 · по дням 2·4·5',  score: 67 },
        // ⚠️ Смещённая выборка: данные только за 14–15.08, два лучших дня после отката.
        // Формула дала бы 100. В групповой балл не идёт — правило от 16.08.
        { name: 'Бодрость пробуждения', value: '⚠️ 100, но только 2 дня из 6 — оба лучшие', score: 100, stale: true },
        { name: 'Обязательность',    value: '⚠️ 100, но только 3 дня из 6', score: 100, stale: true }
      ]
    },
    {
      key: 'food', name: 'Питание', icon: '◐',
      // ⚠️ ВСЯ ГРУППА посчитана по трём дням из шести (13–15.08). За 10–12.08 еда не собиралась.
      scales: [
        { name: 'Вода',              value: '≈1600 мл · «много/немного», цифры нет', score: 64, est: true },
        { name: 'Без срывов',        value: '2 чистых дня из 3 · печеньки 14.08', score: 67, est: true },
        { name: 'Регулярность',      value: '89% приёмов · обед ровный, завтрак плавает', score: 83, est: true },
        { name: 'Правильность еды',  value: 'преобладает 🟡, зелёных почти нет', score: 50, est: true },
        { name: 'Калории',           value: '≈1730 из 2900 · по дням 1330·1750·2110', score: 60, est: true }
      ]
    },
    {
      key: 'sport', name: 'Спорт', icon: '▲',
      // Вся группа — объективный Notion Workout Log. Нули здесь настоящие, не дыра.
      // ⚠️ ГРУППА ВЫРОСЛА ЛОЖНО: весь рост дала шкала «Боли» (спина перестала болеть,
      // потому что четыре дня он лежал), а профильная «Стабильность спорта» упала.
      scales: [
        { name: 'Выносливость',      value: '10 км за 22:11 = 27.0 км/ч · замер 19.07', score: 90 },
        // Болей нет ни в один из измеримых дней (13–15.08).
        // ⚠️ формула по-прежнему без градации — под правку в аудите.
        { name: 'Боли',              value: '0 дней с болью · ⚠️ спина отдыхала лёжа', score: 100 },
        { name: 'Силовые',           value: '20 подтягиваний',score: 80, stale: true },
        // зарядка 2/6 · силовые 1/3 · вел 1/2 → 33×0.4 + 33×0.4 + 50×0.2
        { name: 'Стабильность спорта',value: 'зарядка 2/6 · силовая 1/3 · вел 1/2 · 0 из 12 за 10–13.08', score: 36 },
        { name: 'Осанка',            value: 'плечо вперёд',   score: 40 },
        { name: 'Гибкость',          value: 'не мерили',      score: null },
        { name: 'Карта мышц',        value: 'не сделана',     score: null }
      ]
    },
    {
      key: 'health', name: 'Здоровье', icon: '＋',
      scales: [
        { name: 'Больные дни',       value: '0 за неделю',    score: 100 },
        { name: 'Вес',               value: '74.1 кг · замер 02.08', score: null, raw: true, note: 'цель 78–80' },
        { name: 'Замеры тела',       value: 'не мерили',      score: null },
        { name: 'Ногти',             value: '1/5 · грызёт постоянно', score: 20 },
        { name: 'Кожа лица',         value: '4/5 · без воспалений',   score: 80 },
        // 02.08: 4 забора INVITRO. Формула: 100 − 30×(вне нормы) − 10×(у границы) по последнему.
        // 28.07: вне нормы витамин D (20.5) → −30; у границы креатинин 98 → −10. Итого 60.
        { name: 'Витамины · анализы',value: 'D 20.5 ↓ с 52.9 · 1 из 8 вне нормы', score: 60 },
        // 🆕 Шкала введена 09.08.2026. Причина: курс витамина D шёл неделю и был полностью
        // невидим в рейтинге — 0 из 7 нигде не отражались. Тип B, % дней приёма.
        // В прошлых картах шкалы не было (курс начат 03.08) — прочерк, пересчёт не нужен.
        { name: 'Приём добавок',     value: 'витамин D 0 из 7 · 4-я неделя, не куплен', score: 0 }
      ]
    },
    {
      key: 'head', name: 'Голова', icon: '◇',
      scales: [
        // 3 подтверждённых дня из 5 (10.08 — выходной от чтения по правилу книги).
        // 11–12.08 не собирались — цифра, вероятно, занижена, но выдумывать не буду.
        { name: 'Чтение',            value: '3 дня из 5 · 13.08 читал в худший день', score: 60, est: true },
        // 34.5 ч экрана за неделю, из них 27 ч за три дня отката по девять подряд.
        { name: 'Экранная гигиена',  value: '≈1 чистый вечер из 6 · 34.5 ч экрана', score: 17 },
        // 0.6 × 50% (вечерняя, 3 из 6) + 0.4 × 33% (письменная, 1 из 3) = 43.
        { name: 'Осознанность',      value: 'вечерняя 3/6 · письменная 1/3', score: 43 },
        { name: 'Музыка',            value: 'данные есть, формулы нет', score: null }
      ]
    },
    {
      key: 'spirit', name: 'Дух', icon: '☾',
      scales: [
        // Медитация за неделю не упоминалась ни разу — прочерк, не ноль.
        { name: 'Медитация',         value: 'не упоминалась', score: null },
        // Шли даже 13.08, в худший день недели — самая живучая практика.
        { name: 'Аффирмации',        value: '3 дня из 6 · держались и в откате', score: 50, est: true }
      ]
    }
  ]
};

/* Планка на сейчас — куда тянемся этим месяцем.
   Растёт по принципу растущей планки, когда держится. */
var TARGET = 70;

/* ─── Зоны балла ─────────────────────────────────────────── */
var ZONES = [
  { lo: 0,  hi: 39,  name: 'Дно',      v: '--z-red'    },
  { lo: 40, hi: 64,  name: 'Просело',  v: '--z-orange' },
  { lo: 65, hi: 84,  name: 'Рабочее',  v: '--z-yellow' },
  { lo: 85, hi: 100, name: 'Держит',   v: '--z-green'  }
];

function zoneOf(v) {
  if (v === null || v === undefined) return null;
  for (var i = 0; i < ZONES.length; i++) {
    if (v >= ZONES[i].lo && v <= ZONES[i].hi) return ZONES[i];
  }
  return ZONES[ZONES.length - 1];
}

function colorOf(v) {
  var z = zoneOf(v);
  return z ? 'var(' + z.v + ')' : 'var(--fade)';
}

/* ─── Расчёт по правилам skill rating-calc ───────────────── */

// балл группы = среднее живых баллов (устаревшие и пустые не участвуют).
// Меньше двух живых шкал — балла нет: одна шкала за целую область врёт.
var MIN_LIVE = 2;

function groupScore(g) {
  var live = g.scales.filter(function (s) {
    return s.score !== null && s.score !== undefined && !s.stale && !s.raw;
  });
  if (live.length < MIN_LIVE) return null;
  var sum = live.reduce(function (a, s) { return a + s.score; }, 0);
  return Math.round(sum / live.length);
}

function groupCounts(g) {
  var live = 0, stale = 0, empty = 0;
  g.scales.forEach(function (s) {
    var has = (s.score !== null && s.score !== undefined) || s.raw;
    if (!has) empty++;
    else if (s.stale) stale++;
    else live++;
  });
  return { live: live, stale: stale, empty: empty, total: g.scales.length };
}

// Веса групп (решение Саши, 27.07.2026). Простое среднее врало:
// «Дух» — 2 незапущенные шкалы — весил столько же, сколько «Спорт» с семью,
// и ронял индекс на 7 пунктов. Вес — про важность области, не про число шкал.
var WEIGHTS = {
  core: 2, stability: 2, food: 1.5, sport: 1.5, health: 1, head: 1, spirit: 0.5
};

function overall() {
  var scores = [], noSpirit = [];
  RATING.groups.forEach(function (g) {
    var s = groupScore(g);
    if (s === null) return;
    var w = WEIGHTS[g.key] || 1;
    scores.push({ s: s, w: w });
    if (g.key !== 'spirit') noSpirit.push({ s: s, w: w });
  });
  var avg = function (a) {
    if (!a.length) return null;
    var sum = a.reduce(function (x, i) { return x + i.s * i.w; }, 0);
    var wsum = a.reduce(function (x, i) { return x + i.w; }, 0);
    return Math.round(sum / wsum);
  };
  var live = 0, stale = 0, empty = 0, total = 0;
  RATING.groups.forEach(function (g) {
    var c = groupCounts(g);
    live += c.live; stale += c.stale; empty += c.empty; total += c.total;
  });
  return {
    index: avg(scores),
    indexNoSpirit: avg(noSpirit),
    live: live, stale: stale, empty: empty, total: total,
    coverage: Math.round(live / total * 100)
  };
}

/* ─── Тема: системная + страховка ?theme=dark|light ──────── */
(function () {
  var m = location.search.match(/theme=(dark|light)/);
  if (m) document.documentElement.setAttribute('data-theme', m[1]);
})();
